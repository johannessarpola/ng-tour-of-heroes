import { RealthreatDirective } from './realthreat.directive';

describe('RealthreatDirective', () => {
  it('should create an instance', () => {
    const directive = new RealthreatDirective();
    expect(directive).toBeTruthy();
  });
});
